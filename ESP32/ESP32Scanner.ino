/*
Legacy manual serial experiment kept only as a placeholder.

The production ESP32 firmware entrypoint is MMScanner.ino.
This file intentionally contains no setup()/loop() definitions so it does not
compete with the main sketch during Arduino builds.
*/
